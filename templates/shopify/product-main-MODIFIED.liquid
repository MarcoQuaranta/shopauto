{% if template.suffix != 'landing' %}

  {%- comment -%}
    *** CODICE ORIGINALE DEL TEMA ***
    Shopify qui dentro ricostruisce automaticamente il layout originale del prodotto.
    NON cancellare questo commento, Shopify lo userà per rigenerare la sezione.
  {%- endcomment -%}

  {% render 'product-template' %}

{% endif %}



{% if template.suffix == 'landing' %}

{%- comment -%}
  === LANDING PAGE GENERATA DALL'APP ===
  I contenuti vengono letti dai metafields product.metafields.landing.*
{%- endcomment -%}

<style>
 * { margin: 0; padding: 0; box-sizing: border-box; }
 body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; }
 .lp-width { max-width: 1000px; margin: 0 auto; padding: 0 20px; }
 .text-center { text-align: center; }
 .icon-box {
   display: flex;
   align-items: center;
   gap: 10px;
   font-size: 13px;
   font-weight: 500;
   color: #555;
   letter-spacing: 0.02em;
 }
 .icon-box svg {
   width: 20px;
   height: 20px;
   stroke: #666;
 }
 h2 {
   font-size: 2.2rem;
   margin-bottom: 30px;
   font-weight: 800;
   line-height: 1.2;
   color: #000;
 }

 /* --- COMPONENTS --- */
 .scarcity-bar {
   background-color: #0d1b2a;
   color: #fff;
   text-align: center;
   padding: 12px;
   font-size: 13px;
   font-weight: 600;
   letter-spacing: 1px;
   text-transform: uppercase;
 }

 .hero-section {
   padding: 60px 0 80px 0;
   background: linear-gradient(to bottom, #f4f6f8, #ffffff);
 }
 .hero-grid {
   display: grid;
   grid-template-columns: 1fr 1fr;
   gap: 60px;
   align-items: flex-start;
 }
 .hero-img img {
   width: 100%;
   border-radius: 12px;
   box-shadow: 0 20px 40px rgba(0,0,0,0.08);
   display: block;
 }
 h1.lp-title {
   font-size: 3rem;
   line-height: 1.1;
   margin-bottom: 20px;
   color: #000;
   font-weight: 900;
   letter-spacing: -1.5px;
 }
 p.lp-subtitle {
   font-size: 1.3rem;
   color: #555;
   margin-bottom: 35px;
   line-height: 1.5;
 }

 .price-box {
   background: #fff;
   padding: 35px;
   border: 1px solid #eee;
   border-radius: 16px;
   box-shadow: 0 20px 60px rgba(0,0,0,0.08);
 }
 .price-row {
   display: flex;
   align-items: center;
   justify-content: flex-start;
   margin-bottom: 20px;
 }
 .price-display {
   font-size: 2.8rem;
   font-weight: 800;
   color: #000;
   letter-spacing: -1px;
   line-height: 1;
 }
 .strike {
   text-decoration: line-through;
   opacity: 0.5;
   font-size: 1rem;
   margin-left: 12px;
   font-weight: 400;
 }

 /* QUANTITY SELECTOR */
 .qty-wrapper {
   display: flex;
   align-items: center;
   justify-content: space-between;
   padding: 12px 16px;
   background: #f8f8f8;
   border-radius: 8px;
   margin-bottom: 16px;
 }
 .qty-label {
   font-size: 13px;
   font-weight: 600;
   color: #333;
   text-transform: uppercase;
   letter-spacing: 0.5px;
 }
 .qty-controls {
   display: flex;
   align-items: center;
   gap: 0;
   border: 1px solid #ddd;
   border-radius: 6px;
   overflow: hidden;
 }
 .qty-btn {
   width: 40px;
   height: 40px;
   background: #fff;
   border: none;
   font-size: 18px;
   cursor: pointer;
   color: #333;
   transition: background 0.2s;
 }
 .qty-btn:hover { background: #f0f0f0; }
 .qty-input {
   width: 50px;
   height: 40px;
   text-align: center;
   border: none;
   border-left: 1px solid #ddd;
   border-right: 1px solid #ddd;
   font-size: 16px;
   font-weight: 600;
   background: #fff;
 }

 /* KLARNA BOX */
 .klarna-box {
   background: #ffb3c7;
   border: 1px solid #ff99b3;
   border-radius: 8px;
   padding: 14px;
   text-align: center;
   margin-bottom: 16px;
 }
 .klarna-text {
   font-size: 14px;
   color: #1a1a1a;
 }
 .klarna-text strong { font-weight: 700; }

 .shipping-note {
   text-align: center;
   font-size: 13px;
   color: #666;
   margin-top: 12px;
 }

 .cta-button {
   display: block;
   width: 100%;
   background-color: #000;
   color: #fff;
   font-size: 1.2rem;
   font-weight: 800;
   text-align: center;
   padding: 24px;
   border-radius: 8px;
   text-decoration: none;
   text-transform: uppercase;
   border: none;
   cursor: pointer;
   box-shadow: 0 10px 20px rgba(0,0,0,0.15);
   transition: all 0.3s;
 }
 .cta-button:hover {
   background-color: #333;
   transform: translateY(-2px);
 }

 .icons-row {
   display: flex;
   justify-content: center;
   align-items: center;
   gap: 40px;
   padding: 35px 20px;
   background: transparent;
 }
 .icons-row .divider {
   width: 1px;
   height: 20px;
   background: #ddd;
 }

 .feature-section {
   padding: 80px 0;
   background: #f9f9f9;
 }
 .feature-row {
   display: grid;
   grid-template-columns: 1fr 1fr;
   gap: 70px;
   align-items: center;
   margin-bottom: 60px;
 }
 .feature-row:nth-child(even) {
   direction: rtl;
 }
 .feature-row:nth-child(even) > * {
   direction: ltr;
 }
 .feature-img img {
   width: 100%;
   border-radius: 12px;
   box-shadow: 0 10px 30px rgba(0,0,0,0.1);
 }
 .feature-text h3 {
   font-size: 2rem;
   margin-bottom: 20px;
   font-weight: 800;
 }
 .feature-text p {
   font-size: 1.1rem;
   line-height: 1.7;
   color: #555;
 }

 .reviews-section {
   background: #f4f4f4;
   padding: 80px 0;
   margin-top: 50px;
 }

 /* TEXT BLOCK SECTION */
 .text-block-section {
   background: #4a4a4a;
   padding: 80px 0;
 }
 .text-block-content {
   text-align: center;
   margin-bottom: 60px;
 }
 .text-block-subtitle {
   font-size: 14px;
   font-weight: 500;
   color: rgba(255, 255, 255, 0.7);
   text-transform: uppercase;
   letter-spacing: 0.1em;
   margin-bottom: 20px;
 }
 .text-block-description {
   font-size: 32px;
   font-weight: 300;
   color: #ffffff;
   line-height: 1.5;
   margin: 0 auto;
   max-width: 800px;
 }
 .text-block-image {
   display: flex;
   justify-content: center;
 }
 .text-block-image img {
   width: 300px;
   height: 300px;
   border-radius: 50%;
   object-fit: cover;
 }

 /* FAQ SECTION */
 .faq-section {
   background: #f8f9fa;
   padding: 80px 0;
 }
 .faq-title {
   font-size: 36px;
   font-weight: bold;
   color: #1a1a1a;
   text-align: center;
   margin-bottom: 50px;
 }
 .faq-list {
   max-width: 800px;
   margin: 0 auto;
   display: flex;
   flex-direction: column;
   gap: 16px;
 }
 .faq-item {
   background: #ffffff;
   border-radius: 12px;
   overflow: hidden;
   box-shadow: 0 2px 8px rgba(0,0,0,0.06);
 }
 .faq-question {
   width: 100%;
   display: flex;
   justify-content: space-between;
   align-items: center;
   padding: 24px;
   background: none;
   border: none;
   cursor: pointer;
   text-align: left;
   font-size: 18px;
   font-weight: 600;
   color: #1a1a1a;
 }
 .faq-question:hover {
   background: #f8f9fa;
 }
 .faq-icon {
   width: 24px;
   height: 24px;
   transition: transform 0.3s ease;
 }
 .faq-item.active .faq-icon {
   transform: rotate(180deg);
 }
 .faq-answer {
   max-height: 0;
   overflow: hidden;
   transition: max-height 0.3s ease;
 }
 .faq-item.active .faq-answer {
   max-height: 500px;
 }
 .faq-answer p {
   padding: 0 24px 24px;
   margin: 0;
   font-size: 16px;
   line-height: 1.7;
   color: #4a4a4a;
 }

 /* ABOUT SECTION */
 .about-section {
   background: #ffffff;
   padding: 80px 0;
   border-top: 1px solid #eee;
 }
 .about-title {
   font-size: 36px;
   font-weight: bold;
   color: #1a1a1a;
   margin-bottom: 24px;
   text-align: center;
 }
 .about-description {
   font-size: 18px;
   line-height: 1.8;
   color: #4a4a4a;
   margin-bottom: 20px;
   text-align: center;
   max-width: 700px;
   margin-left: auto;
   margin-right: auto;
 }
 .about-values {
   display: grid;
   grid-template-columns: repeat(2, 1fr);
   gap: 30px;
   margin-top: 50px;
   max-width: 900px;
   margin-left: auto;
   margin-right: auto;
 }
 .value-item {
   display: flex;
   align-items: flex-start;
   gap: 16px;
   padding: 24px;
   background: #f8f9fa;
   border-radius: 12px;
 }
 .value-icon {
   width: 48px;
   height: 48px;
   background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
   border-radius: 12px;
   display: flex;
   align-items: center;
   justify-content: center;
   flex-shrink: 0;
 }
 .value-icon svg {
   width: 24px;
   height: 24px;
   stroke: #ffffff;
 }
 .value-text strong {
   display: block;
   font-size: 16px;
   font-weight: 600;
   color: #1a1a1a;
   margin-bottom: 4px;
 }
 .value-text span {
   font-size: 14px;
   color: #6b7280;
 }

 @media (max-width: 768px) {
   .text-block-description {
     font-size: 24px;
   }
   .text-block-image img {
     width: 220px;
     height: 220px;
   }
   .faq-question {
     font-size: 16px;
     padding: 20px;
   }
   .about-values {
     grid-template-columns: 1fr;
   }
 }
 .review-grid {
   display: grid;
   grid-template-columns: 1fr 1fr;
   gap: 30px;
 }
 .review-card {
   background: white;
   padding: 40px;
   border-radius: 16px;
   box-shadow: 0 5px 20px rgba(0,0,0,0.03);
 }

 .sticky-cta {
   position: fixed;
   bottom: 0;
   left: 0;
   width: 100%;
   background: #fff;
   padding: 16px 20px;
   box-shadow: 0 -4px 20px rgba(0,0,0,0.1);
   z-index: 9999;
   border-top: 1px solid #eee;
 }
 .sticky-content {
   max-width: 1000px;
   margin: 0 auto;
   display: flex;
   align-items: center;
   justify-content: space-between;
   gap: 20px;
 }
 .sticky-info {
   display: flex;
   flex-direction: column;
   gap: 2px;
 }
 .sticky-price {
   font-size: 1.4rem;
   font-weight: 700;
   color: #000;
 }
 .sticky-klarna {
   font-size: 12px;
   color: #666;
 }
 .sticky-button {
   background: #000;
   color: #fff;
   padding: 14px 32px;
   font-size: 14px;
   font-weight: 700;
   text-transform: uppercase;
   border: none;
   border-radius: 6px;
   cursor: pointer;
   transition: background 0.2s;
 }
 .sticky-button:hover { background: #333; }

 @media (max-width: 768px) {
   .hero-grid,
   .feature-row,
   .review-grid {
     grid-template-columns: 1fr;
     gap: 30px;
   }
   .icons-row {
     flex-direction: column;
     gap: 20px;
   }
   .icons-row .divider {
     display: none;
   }
   h1.lp-title {
     font-size: 2rem;
   }
   .feature-row:nth-child(even) {
     direction: ltr;
   }
   .sticky-content {
     flex-direction: column;
     gap: 12px;
   }
   .sticky-button {
     width: 100%;
   }
   .sticky-info {
     align-items: center;
   }
 }
</style>

<div class="lp-container">

  {%- comment -%} SCARCITY BAR {%- endcomment -%}
  {% if product.metafields.landing.scarcity_text %}
    <div class="scarcity-bar">{{ product.metafields.landing.scarcity_text }}</div>
  {% else %}
    <div class="scarcity-bar">💎 Ultime unità disponibili</div>
  {% endif %}

  {%- comment -%} HERO SECTION {%- endcomment -%}
  <div class="hero-section">
    <div class="lp-width hero-grid">

      <div class="hero-img">
        {% if product.metafields.landing.hero_image %}
          <img src="{{ product.metafields.landing.hero_image }}" alt="{{ product.title }}">
        {% else %}
          <img src="{{ product.featured_image | img_url: '1000x' }}" alt="{{ product.title }}">
        {% endif %}
      </div>

      <div>
        {% if product.metafields.landing.hero_title %}
          <h1 class="lp-title">{{ product.metafields.landing.hero_title }}</h1>
        {% else %}
          <h1 class="lp-title">{{ product.title }}</h1>
        {% endif %}

        {% if product.metafields.landing.hero_subtitle %}
          <p class="lp-subtitle">{{ product.metafields.landing.hero_subtitle }}</p>
        {% else %}
          <p class="lp-subtitle">Eleganza senza tempo.</p>
        {% endif %}

        <div class="price-box">
          <div class="price-row">
            <span class="price-display">{{ product.price | money }}</span>
            {% if product.compare_at_price_max > product.price %}
              <span class="strike">{{ product.compare_at_price_max | money }}</span>
            {% endif %}
          </div>

          {%- comment -%} QUANTITY SELECTOR {%- endcomment -%}
          <div class="qty-wrapper">
            <span class="qty-label">Quantità</span>
            <div class="qty-controls">
              <button type="button" class="qty-btn" onclick="changeQty(-1)">−</button>
              <input type="number" id="hero-qty" class="qty-input" value="1" min="1" readonly>
              <button type="button" class="qty-btn" onclick="changeQty(1)">+</button>
            </div>
          </div>

          {%- comment -%} KLARNA BOX {%- endcomment -%}
          <div class="klarna-box">
            <span class="klarna-text">Paga in 3 rate da <strong>{{ product.price | divided_by: 3 | money }}</strong> con Klarna</span>
          </div>

          <button onclick="buyNow()" class="cta-button">
            {% if product.metafields.landing.cta_button_text %}
              {{ product.metafields.landing.cta_button_text }}
            {% else %}
              ORDINA ORA
            {% endif %}
          </button>

          <p class="shipping-note">Spedizione gratuita oggi</p>
        </div>

      </div>
    </div>
  </div>

  {%- comment -%} ICONS ROW {%- endcomment -%}
  <div class="icons-row lp-width">
    <div class="icon-box">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
      </svg>
      <span>{% if product.metafields.landing.icon1_text %}{{ product.metafields.landing.icon1_text }}{% else %}Qualità Garantita{% endif %}</span>
    </div>
    <div class="divider"></div>
    <div class="icon-box">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <span>{% if product.metafields.landing.icon2_text %}{{ product.metafields.landing.icon2_text }}{% else %}Garanzia 2 Anni{% endif %}</span>
    </div>
    <div class="divider"></div>
    <div class="icon-box">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"></path>
      </svg>
      <span>{% if product.metafields.landing.icon3_text %}{{ product.metafields.landing.icon3_text }}{% else %}Pagamento Sicuro{% endif %}</span>
    </div>
  </div>

  {%- comment -%} FEATURE SECTIONS {%- endcomment -%}
  <div class="feature-section">
    <div class="lp-width">
      <div class="feature-row">
        <div class="feature-text">
          {% if product.metafields.landing.section1_title %}
            <h3>{{ product.metafields.landing.section1_title }}</h3>
          {% else %}
            <h3>Qualità Superiore</h3>
          {% endif %}
          {% if product.metafields.landing.section1_text %}
            <p>{{ product.metafields.landing.section1_text }}</p>
          {% else %}
            <p>Un prodotto realizzato con materiali di prima qualità, pensato per durare nel tempo e offrirti sempre il massimo delle prestazioni.</p>
          {% endif %}
        </div>
        <div class="feature-img">
          {% if product.metafields.landing.section1_image %}
            <img src="{{ product.metafields.landing.section1_image }}" alt="{{ product.metafields.landing.section1_title }}">
          {% else %}
            <img src="{{ product.featured_image | img_url: '800x' }}" alt="{{ product.title }}">
          {% endif %}
        </div>
      </div>
    </div>
  </div>

  {% if product.metafields.landing.section2_title or product.metafields.landing.section2_text %}
    <div class="feature-section" style="background: #fff;">
      <div class="lp-width">
        <div class="feature-row">
          <div class="feature-img">
            {% if product.metafields.landing.section2_image %}
              <img src="{{ product.metafields.landing.section2_image }}" alt="{{ product.metafields.landing.section2_title }}">
            {% endif %}
          </div>
          <div class="feature-text">
            {% if product.metafields.landing.section2_title %}
              <h3>{{ product.metafields.landing.section2_title }}</h3>
            {% endif %}
            {% if product.metafields.landing.section2_text %}
              <p>{{ product.metafields.landing.section2_text }}</p>
            {% endif %}
          </div>
        </div>
      </div>
    </div>
  {% endif %}

  {% if product.metafields.landing.section3_title or product.metafields.landing.section3_text %}
    <div class="feature-section">
      <div class="lp-width">
        <div class="feature-row">
          <div class="feature-img">
            {% if product.metafields.landing.section3_image %}
              <img src="{{ product.metafields.landing.section3_image }}" alt="{{ product.metafields.landing.section3_title }}">
            {% endif %}
          </div>
          <div class="feature-text">
            {% if product.metafields.landing.section3_title %}
              <h3>{{ product.metafields.landing.section3_title }}</h3>
            {% endif %}
            {% if product.metafields.landing.section3_text %}
              <p>{{ product.metafields.landing.section3_text }}</p>
            {% endif %}
          </div>
        </div>
      </div>
    </div>
  {% endif %}

  {%- comment -%} TEXT BLOCK SECTION {%- endcomment -%}
  <div class="text-block-section">
    <div class="lp-width">
      <div class="text-block-content">
        <p class="text-block-subtitle">
          {% if product.metafields.landing.text_block_subtitle %}
            {{ product.metafields.landing.text_block_subtitle }}
          {% else %}
            La nostra promessa
          {% endif %}
        </p>
        <h2 class="text-block-description">
          {% if product.metafields.landing.text_block_description %}
            {{ product.metafields.landing.text_block_description }}
          {% else %}
            Un prodotto pensato per chi non si accontenta. Ogni dettaglio è stato curato con la massima attenzione per offrirti un'esperienza unica e indimenticabile.
          {% endif %}
        </h2>
      </div>
      <div class="text-block-image">
        {% if product.metafields.landing.text_block_image %}
          <img src="{{ product.metafields.landing.text_block_image }}" alt="{{ product.title }}">
        {% endif %}
      </div>
    </div>
  </div>

  {%- comment -%} FAQ SECTION {%- endcomment -%}
  <div class="faq-section">
    <div class="lp-width">
      <h2 class="faq-title">Domande Frequenti</h2>
      <div class="faq-list">
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFaq(this)">
            <span>Quali sono i tempi di spedizione?</span>
            <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div class="faq-answer">
            <p>Le spedizioni vengono elaborate entro 24-48 ore lavorative. La consegna avviene in 3-5 giorni lavorativi per l'Italia. Per le isole e zone remote potrebbero essere necessari 1-2 giorni aggiuntivi.</p>
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFaq(this)">
            <span>Posso effettuare un reso?</span>
            <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div class="faq-answer">
            <p>Certamente! Hai 30 giorni di tempo dalla ricezione del prodotto per effettuare un reso. Il prodotto deve essere nelle condizioni originali con tutti gli imballaggi. Contattaci via email per avviare la procedura.</p>
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFaq(this)">
            <span>Quali metodi di pagamento accettate?</span>
            <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div class="faq-answer">
            <p>Accettiamo tutte le principali carte di credito e debito (Visa, Mastercard, American Express), PayPal, Apple Pay, Google Pay e bonifico bancario. Tutti i pagamenti sono sicuri e crittografati.</p>
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFaq(this)">
            <span>Il prodotto ha una garanzia?</span>
            <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div class="faq-answer">
            <p>Tutti i nostri prodotti sono coperti da una garanzia di 24 mesi dalla data di acquisto. La garanzia copre difetti di fabbricazione e malfunzionamenti. Per attivare la garanzia conserva la ricevuta d'acquisto.</p>
          </div>
        </div>
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFaq(this)">
            <span>Come posso contattare l'assistenza clienti?</span>
            <svg class="faq-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div class="faq-answer">
            <p>Il nostro team di assistenza clienti è disponibile dal lunedì al venerdì, dalle 9:00 alle 18:00. Puoi contattarci via email all'indirizzo supporto@tuonegozio.it o tramite il modulo di contatto sul sito. Rispondiamo entro 24 ore.</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  {%- comment -%} ABOUT SECTION {%- endcomment -%}
  <div class="about-section">
    <div class="lp-width">
      <h2 class="about-title">Chi Siamo</h2>
      <p class="about-description">
        Siamo un team appassionato dedicato a offrirti prodotti di alta qualità che fanno la differenza nella tua vita quotidiana. La nostra missione è semplice: selezionare solo il meglio per te.
      </p>
      <p class="about-description">
        Con anni di esperienza nel settore, abbiamo costruito relazioni dirette con i migliori produttori per garantirti autenticità, qualità e prezzi competitivi.
      </p>
      <div class="about-values">
        <div class="value-item">
          <div class="value-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <div class="value-text">
            <strong>Qualità Garantita</strong>
            <span>Prodotti selezionati con cura</span>
          </div>
        </div>
        <div class="value-item">
          <div class="value-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <div class="value-text">
            <strong>Spedizione Veloce</strong>
            <span>Consegna in 3-5 giorni</span>
          </div>
        </div>
        <div class="value-item">
          <div class="value-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
            </svg>
          </div>
          <div class="value-text">
            <strong>Clienti Soddisfatti</strong>
            <span>Oltre 10.000 recensioni positive</span>
          </div>
        </div>
        <div class="value-item">
          <div class="value-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
            </svg>
          </div>
          <div class="value-text">
            <strong>Pagamenti Sicuri</strong>
            <span>Transazioni 100% protette</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  {%- comment -%} REVIEWS SECTION {%- endcomment -%}
  <div class="reviews-section">
    <div class="lp-width">
      <h2 class="text-center">
        {% if product.metafields.landing.reviews_title %}
          {{ product.metafields.landing.reviews_title }}
        {% else %}
          Recensioni Clienti
        {% endif %}
      </h2>
      <div class="review-grid">
        <div class="review-card">
          {% if product.metafields.landing.review1_text %}
            {{ product.metafields.landing.review1_text }}
          {% else %}
            ★★★★★ – "Stupendo"
          {% endif %}
        </div>
        <div class="review-card">
          {% if product.metafields.landing.review2_text %}
            {{ product.metafields.landing.review2_text }}
          {% else %}
            ★★★★★ – "Arrivato subito e bellissimo"
          {% endif %}
        </div>
      </div>
    </div>
  </div>

  {%- comment -%} STICKY CTA {%- endcomment -%}
  <div class="sticky-cta">
    <div class="sticky-content">
      <div class="sticky-info">
        <span class="sticky-price">{{ product.price | money }}</span>
        <span class="sticky-klarna">o 3 rate da {{ product.price | divided_by: 3 | money }} con Klarna</span>
      </div>
      <button onclick="buyNow()" class="sticky-button">
        {% if product.metafields.landing.sticky_cta_text %}
          {{ product.metafields.landing.sticky_cta_text }}
        {% else %}
          ACQUISTA ORA
        {% endif %}
      </button>
    </div>
  </div>

</div>

<script>
 var heroQty = 1;

 function changeQty(delta) {
   heroQty = Math.max(1, heroQty + delta);
   document.getElementById('hero-qty').value = heroQty;
 }

 function buyNow() {
   var variantId = {{ product.selected_or_first_available_variant.id }};
   var qty = parseInt(document.getElementById('hero-qty').value) || 1;
   fetch('/cart/clear.js', { method: 'POST' })
   .then(function() {
     return fetch('/cart/add.js', {
       method: 'POST',
       headers: { 'Content-Type': 'application/json' },
       body: JSON.stringify({ items: [{ id: variantId, quantity: qty }] })
     });
   })
   .then(function() { window.location.href = '/checkout'; });
 }

 function toggleFaq(button) {
   var item = button.parentElement;
   var isActive = item.classList.contains('active');
   document.querySelectorAll('.faq-item').forEach(function(el) {
     el.classList.remove('active');
   });
   if (!isActive) {
     item.classList.add('active');
   }
 }
</script>

{% endif %}



{% schema %}
{
  "name": "Product Template",
  "settings": []
}
{% endschema %}
